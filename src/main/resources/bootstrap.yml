spring:
  application:
    name: configserver
encrypt:
  failOnError: false
  keyStore:
    location: classpath:/root/keystore.jks
    alias: homepage
    password: ${KEYSTORE_PASSWORD}
    secret: ${KEYSTORE_PASSWORD}